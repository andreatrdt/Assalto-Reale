# Python to Web Parity Matrix

Status legend:

- Complete: implemented in TypeScript and covered by parity or controller tests.
- Partial: implemented, but simplified, not fully Python-equivalent, or not fully exposed in UI.
- Missing: no meaningful web implementation yet.
- Untested: implementation may exist, but parity is not proven against Python.
- Incorrect: current web behaviour conflicts with Python and must be changed.

Current web baseline: `refactor/modern-web-game` at `2760a8f`.

| System | Python source | Python behaviour | Current web behaviour | Parity status | Required change | Tests required |
| --- | --- | --- | --- | --- | --- | --- |
| Board configuration | `assalto_core.py:35`, `README.md` | Default active game is 12x12, 5 Special Squares, Black/White, Transform off by default. | TypeScript config is 12x12, 5 specials, Black/White, Transform off. | Complete | Keep config centralized in engine, not UI/store. | Fixture for default config and snapshot round trip. |
| Piece counts | `README.md`, `Board.PIECE_COUNTS` | Each player has 1 King, 4 Attack, 4 Defense, 4 Conquest. | Web placement state uses same counts. | Complete | Move setup counts into setup module during refactor. | Placement completion fixture and store setup tests. |
| Special Square generation | `assalto_core.py:314-341` | Random generation uses Python `random.Random(seed)`, candidate rows 1..rows-2, cols 3..cols-4 on 12x12, min Chebyshev spacing 3, atomic failure. | TS now owns candidate area, spacing and atomic generation, and normal web matches no longer use hardcoded coordinates. Seeded shuffle is deterministic but not yet proven Python-RNG equivalent. | Partial | Add Python seeded-output fixtures and either port Python RNG exactly or fixture-proof every supported seeded path. | Fixtures for count, area, spacing, seeds, atomic failure/no partial mutation. |
| Normal movement | `assalto_core.py:674-707`, `tests/test_canonical_engine.py` | All pieces move one adjacent empty square in 8 directions, cost 1. | TS `buildAction` matches; limited fixtures only include one move. | Partial | Expand fixtures for all piece/direction cases. | Generated fixtures and TS tests for all four piece types in all 8 directions. |
| Board boundaries | `assalto_core.py:684-687` | Out-of-bounds start/end rejected as `"outside board"`. | TS checks bounds. | Untested | Add generated fixture coverage. | Boundary action fixtures. |
| Friendly occupied destination | `assalto_core.py:710-711` | Rejected with friendly-piece error. | TS rejects friendly destination. | Untested | Add generated fixture coverage. | Friendly destination fixture. |
| Illegal long normal movement | `assalto_core.py:698-701` | Empty destination beyond adjacent rejected. | TS rejects long normal move. | Untested | Add generated fixture coverage. | Long movement fixture. |
| No action points remaining | `assalto_core.py:683-684` | Any action when `moves_this_turn >= 2` rejected. | TS rejects. | Untested | Add generated fixture coverage. | No-AP fixture. |
| Capture hierarchy | `assalto_core.py:424-437`, `tests/test_canonical_engine.py` | Attack captures Defense/King; Defense captures Conquest; Conquest captures Attack; King captures pawns; no King captures King. | TS hierarchy matches. | Partial | Expand fixtures for every legal/illegal pair. | Legal and illegal attacker-target fixtures. |
| Attack Pawn captures | `assalto_core.py:640-650` | Orthogonal only, distance 1 or 2, distance 2 costs 2, clear intermediate, first action. | TS matches core geometry. | Partial | Prove all edge cases by fixtures. | One-square, two-square, blocked, after-first-action, insufficient-AP fixtures. |
| Defense Pawn captures | `assalto_core.py:652-660` | Diagonal only, distance 1 or 2, distance 2 costs 2, clear intermediate, first action. | TS matches core geometry. | Partial | Prove all edge cases by fixtures. | One-square, two-square, blocked, after-first-action, insufficient-AP fixtures. |
| Conquest Pawn captures | `assalto_core.py:662-665` | Captures adjacent Attack Pawns only. | TS matches. | Untested | Add fixtures. | Legal adjacent and illegal target/distance fixtures. |
| King captures | `assalto_core.py:667-670` | Captures adjacent pawns only; cannot capture King; King action limit applies. | TS matches. | Partial | Add fixtures for all pawn types and King target. | King captures pawn fixtures and King-vs-King fixture. |
| Action-point costs | `assalto_core.py:701-744`, `README.md` | Move/capture distance 1 costs 1; range-2 capture costs 2 and ends turn; pass ends turn. | TS action costs match. Store uses returned `nextMovesThisTurn`. | Partial | Add fixtures and controller tests for same non-King piece using both AP. | Action-cost transition fixtures and store turn tests. |
| King action limit | `assalto_core.py:690-692` | King may act once per turn; other piece can act after King if AP remains. | TS action check exists. Store tracks `kingMoved`. | Partial | Add fixtures for King first action, King second action, other piece after King. | Engine fixtures and controller tests. |
| Pass | `assalto_core.py:779`, `842-858` | Pass action ends turn and emits pass event. | Web pass exists in store and TS applyAction. | Partial | Confirm event/result parity and turn-counter side effects. | Pass fixture and controller pass test. |
| Turn switching | `assalto_app_ai.py:2253-2281` | `_switch_player` clears previews, swaps player, increments half-turn counter, resets AP and King state, handles Transform generation and territory maturation. | Store has `advanceHalfTurn`, but in store not separated and not fully verified; terminal-victory ordering is suspect. | Partial | Move into match/turn controller and match Python lifecycle exactly. | Controller tests for every end-turn path and victory precedence. |
| Defended King preview | `assalto_core.py:538-599`, `assalto_app_ai.py:1930-2118` | Engine returns preview before resolution; UI shows preview and confirmation. | TS preview exists; UI only enters defender selection for multiple defenders and auto-resolves single defender. | Partial | Always show preview/confirm before resolution, including one defender. | Fixtures for preview fields; E2E for preview/confirm/cancel. |
| Multiple defenders | `assalto_app_ai.py:2089-2208` | Defender owner selects eligible Defense Pawn; AI chooses if defender owner is AI. | Web supports multiple defenders with pending selection, but pending decision lacks unified owner interface. | Partial | Add `PendingDecision.owner`; AI must inspect owner, not currentPlayer. | Controller tests for human/AI defender owner. |
| Invalid defender selection | `assalto_core.py:729-730`, `842-906` | Invalid selected defender rejected. | TS rejects; UI warns if selection not highlighted. | Untested | Add fixture and controller tests. | Invalid defender fixture and UI/controller test. |
| Bounce obstacle behaviour | `assalto_core.py:565-584`, tests | Bounce stops before occupied square; no jump/rotation. | TS preview mirrors logic; limited fixtures cover one obstacle case in Python tests but not generated fixtures. | Partial | Expand fixtures including obstacle and exact path. | Bounce obstacle fixture. |
| Bounce edge behaviour | `assalto_core.py:565-584` | Bounce stops at board edge; last in-bounds empty/origin square is landing. | TS likely matches. | Untested | Add fixture. | Edge bounce fixture. |
| Attacker origin as empty | `assalto_core.py:573-574`, tests | Origin does not block bounce. | TS allows origin in bounce path. | Partial | Add fixture parity. | Origin-as-empty fixture. |
| Intermediate square in two-square defended attack | `tests/test_canonical_engine.py` | Two-square attack path includes intermediate and obeys clear-intermediate rule. | TS computes attack path. | Partial | Add generated fixture. | Two-square defended attack fixture. |
| Bounce onto Transform | `assalto_core.py:596`, tests | Preview marks `triggers_transform`; apply emits `transform_available`. | TS supports preview/event. | Partial | Add generated fixture and store decision-owner handling. | Bounce-to-transform fixture and controller test. |
| Placement sequence | `assalto_core.py:204-229`, `445-457` | Explicit snake schedule totals 13 each. | TS schedule matches. | Complete | Move queue generation out of store. | Fixture for full schedule and TS queue test. |
| Placement restrictions | `assalto_core.py:460-489` | Bounds, occupied, Special, Transform, King side, Attack columns, Conquest distance. | TS placement matches known restrictions. | Partial | Expand fixtures for every restriction. | Placement restriction fixture set. |
| Manual placement UI | `assalto_app_ai.py:1870-1936`, `3278-3303` | Shows current piece, valid squares, invalid reason, undo placement, progress. | Web shows current piece and valid count; remaining pieces/progress/restart are incomplete. | Partial | Complete placement screen and controller module. | Controller and E2E manual-placement tests. |
| Quick Balanced placement | `assalto_app_ai.py:475-542` | Uses Python scoring and placement legality over generated Special Squares. | Web approximates anchors/scoring and uses hardcoded specials. | Incorrect | Port exact Python algorithm into setup module and compare seeded complete boards. | Multi-seed Python fixtures comparing full placement board. |
| Territory control | `assalto_core.py:783-799` | Only Conquest Pawns on Special Squares control. | TS matches. | Partial | Add fixtures for neutral/Black/White control. | Special-control fixtures. |
| Territory claim creation | `assalto_core.py:808-819` | Strict majority creates claim at turn counter and matures at `turn + 2`. | TS matches core. Store triggers at pass/end turn. | Partial | Verify lifecycle points against app flow. | Controller tests and fixtures. |
| Territory response window | `README.md`, `assalto_app_ai.py:2269-2274` | Opponent gets one complete response turn. | Store increments half-turns and matures at +2. | Partial | Verify no extra advancement after terminal victory/transform. | End-turn and victory-precedence tests. |
| Territory cancellation | `assalto_core.py:808-812` | Claim clears when strict majority is lost. | TS matches core. | Untested in web fixtures | Add generated fixture. | Claim cancellation fixture. |
| Territory owner replacement | `assalto_core.py:813-819` | New majority claimant replaces prior claim. | TS core likely matches. | Untested | Add fixture. | Claim replacement fixture. |
| Territory victory | `assalto_core.py:820-821` | Matured claim wins by territory. | TS core and store have basic test. | Partial | Prove precedence and lifecycle in fixtures/E2E. | Maturation, precedence, E2E tests. |
| Victory precedence | `assalto_core.py:824-840`, `README.md` | King capture takes precedence over territory and timeout. | TS action checks King capture before `evaluateVictory`; store may still run half-turn territory after action if result ends turn and victory already exists. | Partial | Ensure no territory/Transform processing after terminal victory if Python does not. | Fixture/controller test for simultaneous king capture/territory. |
| Transform disabled default | `GameConfig`, tests | Transform disabled by default. | TS default disabled. | Complete | Keep setup default off. | Config fixture. |
| Transform activation timing | `assalto_app_ai.py:2269` | Generate after `turn_counter >= TRANSFORM_ROUND * 2` and no Transform Square. | Store has equivalent but in store and seeded differently. | Partial | Move to turn controller and prove timing. | Too-early/at-threshold fixtures and controller tests. |
| Transform Square generation | `assalto_core.py:343-370` | Candidates are empty, non-Special, interior, equidistant to nearest two pawns of different players; Python seeded choice. | TS has candidate logic but seed selection is not Python-compatible. | Incorrect | Implement Python-compatible seeded generation or fixture-backed exact outputs. | Deterministic seed fixtures, restrictions, failure fixtures. |
| Transform selection | `assalto_app_ai.py:2668-2745` | Human must choose a pawn type; options include all pawn types but engine rejects same type. No skip behaviour is implemented. | Web filters out same type and now requires a transform choice; full Python prompt/option parity still needs fixture and E2E proof. | Partial | Ensure modal ownership, AI choice and exact turn consequences match Python. | Controller/E2E transform-required test. |
| Transform relocation | `assalto_core.py:399-415` | After transform, old square cleared and new Transform Square generated with seed if possible. | TS relocates but seed is not Python-compatible. | Partial | Match generator and failure semantics. | Transform relocation fixture. |
| Transform turn-ending | `assalto_core.py:415-422`, app flow | `transform_piece` ends turn; if Transform happened after a non-ending move, app switches player after prompt. | Store approximates with `forceTurnSwitch`. | Partial | Verify exact Python sequence and remove skip path. | Controller tests for ending and non-ending Transform landings. |
| Human vs Human | `assalto_app_ai.py:1048-1056` | Opponent Human gives local Black/White players. | Web default local mode works as shortcut. | Partial | Replace shortcuts with setup flow. | Setup and E2E H2H tests. |
| Human vs Computer | `assalto_app_ai.py:1048-1056`, `3304-3448` | AI can play either side with paced turns and placement. | Web AI is fixed as White and greedy; no setup side selection. | Incorrect | Implement setup, side selection, worker AI, AI placement. | AI as Black/White setup and E2E tests. |
| Human side selection | `assalto_app_ai.py:78-80`, `1048-1052` | Black, White, Random; Random resolves at match start. | Missing normal UI; AI fixed White. | Missing | Add setup state and persisted resolved side. | Setup controller tests. |
| AI difficulties | `assalto_app_ai.py:455-473` | Easy/Medium/Hard set depth, top-k, max sequences, time budget. | Missing; web has greedy picker only. | Missing | Add difficulty module with exact values. | Difficulty config tests. |
| AI turn sequences | `assalto_app_ai.py:3832-4094` | Turn-level minimax, full-turn sequences, TT, time budget. | Missing; web uses deterministic greedy single action. | Missing | Port AI to worker with cancellation. | Search fixture/benchmark comparisons and worker tests. |
| AI defender choice | `assalto_app_ai.py:2210-2248` | AI evaluates defender sacrifices and picks best. | Web picks first defender. | Incorrect | Port defender-choice evaluation. | Controller tests. |
| AI Transform choice | `assalto_app_ai.py:4165-4204` | AI scores transform options by captures/special control. | Web picks first legal transform option. | Incorrect | Port AI auto-transform scoring. | AI transform tests. |
| Timers | `assalto_app_ai.py:68`, `1454-1471`, `2894-2928` | Untimed, 5, 10, 12, 15, 20 min; active human decision time only; AI/placement/menu paused; timeout victory. | Web now has monotonic active-human countdown during playing phase, pauses AI-owned turns and untimed matches, and displays updated clocks. Full pause policy for every modal/navigation/save case is not yet proven. | Partial | Add full canonical clock lifecycle controller, save/load restoration and lifecycle tests. | Timer preset, pause, AI, modal, navigation, timeout, save/load tests. |
| Timeout victory | `assalto_app_ai.py:1454-1471` | If active human clock reaches 0, opponent wins. | Web store now ends the game with timeout victory when the running player clock reaches zero. Precedence with simultaneous action/territory outcomes is not fully proven. | Partial | Add victory precedence tests and production UI flow coverage. | Timeout controller/E2E tests. |
| Undo | `assalto_app_ai.py:1895-1910`, `2285-2390` | Placement and move undo restore snapshot, transform squares, captures, turn state, special control. | Store snapshot undo exists but incomplete for save, modal, timers, AI cancellation. | Partial | Move to history controller and serialize enough state. | Undo placement/move/transform/defended King tests. |
| Save/load | `assalto_app_ai.py:1175-1305`, `1363-1856`, README | Versioned canonical JSON snapshot plus legacy text loader fallback; saves clocks, placement, undo, settings, AI side, victory. | Web now writes validated schema-2 local saves, supports current/local export and JSON import, restores placement state, clocks, setup config, undo history and unresolved modal fields when present. Schema-1 web saves are still accepted with limitations; Python legacy snapshot import is missing and modal restore is not fixture-proven. | Partial | Add persistence modules, migration coverage, Python legacy import and safe modal restoration fixtures. | Save/load mid-turn, placement, modal, clocks, AI, legacy import tests. |
| Legacy snapshot compatibility | `assalto_app_ai.py:1552-1635` | Accepts old text `moves.txt` fallback. | Missing. | Missing | Add legacy import parser where practical. | Legacy fixture import tests. |
| Match reset/rematch | `assalto_app_ai.py:370-417`, `1154-1172`, `1475-1488` | Restart/rematch preserves menu settings, starts new placement/match. | Missing as real UI flow. | Missing | Add restart/rematch commands/screens. | Controller/E2E tests. |
| Return to menu | `assalto_app_ai.py:1125-1127`, `1862-1865` | Returns to start menu and clears overlays safely. | Web Home button sets phase home, but not a full menu/reset policy. | Partial | Implement menu phase with safe AI/timer cancellation. | Home navigation tests. |
| Setup flow | `assalto_app_ai.py:543-918` | Start menu configures timer, opponent, human side, difficulty, Transform, placement, load/rules. | Web has shortcut buttons. | Missing | Build setup screen and summary. | Setup combination tests and E2E. |
| Rules/help | `assalto_app_ai.py:640-737`, README | Multi-section rules with images and detailed explanations. | Web has brief rules text only. | Partial | Implement full rules/help experience using modern UI and assets. | Rules screen E2E. |
| Audio behaviour | `assalto_app_ai.py:1778-1828`, move/capture/victory calls | Audio unlock, move, capture, shield, victory sounds. | Missing. | Missing | Add audio service respecting browser autoplay. | Unit smoke and E2E interaction tests where possible. |
| Responsive desktop/tablet/mobile | `assalto_app_ai.py:180-355`, web design requirements | Pygame responsive layout; web should maintain modern responsive SVG. | Web desktop/mobile smoke passes for current slice. | Partial | Test all phases and modal overlays at mobile/tablet/desktop. | Playwright visual/flow tests. |
| Declared phases | `web/src/app/phases.ts` | Web phase list includes boot/setup/paused/settings/error, etc. | Many declared phases are unused or not real screens. | Missing | Implement each phase as screen or overlay. | Phase routing tests. |
| PWA/deployment | README modern web section | Only after mechanics complete. | Missing. | Deferred | Do after parity. | Manifest/installability/build tests. |

## Highest-priority gaps

1. Expand Python-generated fixtures so TypeScript parity is measured against the canonical engine instead of hand-picked web cases.
2. Port canonical Special Square generation and stop using hardcoded normal-match layouts.
3. Replace approximated Quick Balanced setup with the exact Python algorithm and fixture comparisons.
4. Split rule and match flow out of `gameStore.ts` into engine/setup/match/persistence modules.
5. Remove invented Transform skip behaviour and align Transform decision flow with Python.
6. Replace shortcut buttons with the Python-equivalent setup flow.
7. Replace greedy AI with the Python difficulty/search behaviour in a worker.
